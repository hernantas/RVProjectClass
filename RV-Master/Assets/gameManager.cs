using UnityEngine;
using System.Collections;

public class gameManager : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//GameObject.Find ("Camp fire ready").SetActive (false);
	}

	void OnGUI(){
		//if (GUILayout.Button ("Show the camp fire")) {
		//	GameObject.Find ("Camp fire ready").SetActive (true);
		//}
	}

	// Update is called once per frame
	void Update () {
	
	}
}
